APP_NAME=FastAPI Minimal Chat
APP_HOST=127.0.0.1
APP_PORT=8000

# auto: 有 OPENAI_API_KEY 就走真实模型，没有就走 mock
# openai: 强制使用 OpenAI 兼容接口
# mock: 本地假数据，便于先把接口跑通
LLM_PROVIDER=auto
LLM_MODEL=gpt-4.1-mini

# 如需真实调用，填入你的 key
OPENAI_API_KEY=
# 可选：兼容网关时再设置
OPENAI_BASE_URL=
